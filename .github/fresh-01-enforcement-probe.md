# FRESH-01 enforcement probe

Temporary probe used only to verify that the protected `main` branch cannot be updated without satisfying the mandatory BDB qualification gates. This file must never be merged to `main`.
